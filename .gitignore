# Infraestructura de la fábrica de software — no forma parte del producto
_worktrees/
codigo para auditar/
_modo-publicacion.txt

# Genéricos
.DS_Store
node_modules/
.env
.env.local

# Next.js
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# Convex — código generado a partir de convex/*.ts (mismo criterio que
# /src/generated/prisma: derivado, se regenera con `npx convex dev`/
# `npx convex codegen`, no se trackea).
/convex/_generated/
